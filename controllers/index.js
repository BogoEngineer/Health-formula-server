// Functions for manipulating with models
const adminController = require('./adminController');
const userController = require('./userController');

module.exports = {
    adminController: adminController,
    userController: userController,
};